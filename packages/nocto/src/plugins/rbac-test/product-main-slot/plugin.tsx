import { Text, Container } from "@medusajs/ui"

export const RbacSlot = (order: any) => {
  return (
    <Container>
      <Text>Your custom plugin in a RBAC slot</Text>
    </Container>
  )
}

export default RbacSlot